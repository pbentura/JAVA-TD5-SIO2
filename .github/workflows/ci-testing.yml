# @author : Julien Pechberty
#
# https://help.github.com/en/actions
name: JAVA TD 5 - Tests runners
on:
  push:
    branches:
      - test-xi
jobs:
  run-test:
    name : Run Tests suite
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        java-version: [21]

    steps:
      - name: Check PHP Version
        run: echo "Coucou from ACTIONS"
